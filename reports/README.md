# Reports

Use this folder for project outputs used in the final write-up.

- `figures/`: plots and visualizations.
- `tables/`: metric summaries and comparison tables.

Suggested outputs:
- metric table by pipeline (`WER`, `CER`, `BLEU`, `chrF`)
- metric table by noise condition
- speaker-level error distribution
- examples of hardest utterances

