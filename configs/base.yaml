project:
  name: ru_pl_speech_translation
  random_seed: 42

data:
  sample_rate: 16000
  audio_format: wav
  manifest_path: data/metadata/manifest.csv

evaluation:
  compute_asr_metrics: true
  compute_translation_metrics: true
  metrics:
    - wer
    - cer
    - bleu
    - chrf

